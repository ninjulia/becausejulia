<div class="card bg-light p-0">
    <div class="card-header w-100 fst-italic text-bg-{{ colors[loop.index0] }}">{{ samples.data.tags[1] }} | {{
        samples.data.tags[2] }}
    </div>
    <div class="card-body d-flex flex-column justify-content-between">
        <h2 class="fs-5 card-title">{{ samples.data.headline }}</h2>
        <p class="card-text">{{ samples.data.intro }}</p>
        <a href="{{ samples.url }}" class="align-self-end btn btn-{{ colors[loop.index0] }}">{{ samples.data.button
            }}</a>
    </div>
</div>